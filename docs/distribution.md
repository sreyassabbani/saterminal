# Distribution

- Cross-compiled Bun executables could support macOS, Linux, and Windows on x64 and ARM64; npm would require per-platform packages and a small dispatcher, otherwise binaries should be distributed outside npm.
